# sortbenchmark
Implementation of three sorting algorithms and comparison of their performance.
These algorithms are:
sorting by mounding (heapsort),
- sort by counting sort (counting sort),
- bucket sort (bucket sort),
